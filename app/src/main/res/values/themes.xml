<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.OCRemixPlayer" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/laranja_primario</item>
    </style>

    <style name="Theme.OCRemixPlayer" parent="Base.Theme.OCRemixPlayer" />

    <style name="Theme.OCRemixPlayer.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.OCRemixPlayer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.OCRemixPlayer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>